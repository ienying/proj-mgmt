
1、dock栏 项目管理页面 项目详细信息 模块里的表除了表格、表单的显示操作 在增加一个ai。能实现读取项目数据库所在 schema 中的所有表，然后做分析。
给出结果。
2、dock栏 案例中心页面 用户画像现在画像的编辑输入现在是按照文本输入，现在在增加一种，调用ai能力的输入。
点击后弹出面板。有启动录音和停止录音停止后，录音文件支持下载和上床，同时也支持上次文件、pdf、ppt、Excel
通过浏览器，调用系统的录音，或者将录音问题上传给大模型，将进行文字转换后，用户确认了，在输入。

AI 使用的大模型 在系统设置 增加一个接入大模型设置 类似输入 DeepSeek 的 key就可以接入，也要能接人DeepSeek的语音服务。多用户使用同一个大模型的 key。然后系统后台能看到，用户使用排行榜。


移动端手机构建一个h5,可以在移动端进行登录后，有两个功能，一可以看到待办模块，看到案例中心。其中案例中心支持查看和编辑用户画像。
登录成功之后 显示三个卡片 任务中心 和 案例中心 
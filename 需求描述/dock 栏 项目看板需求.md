

# dock 栏项目看板按照下面要求优化一下

> 严格按照以下规范生成页面。所有数据来源于项目管理中各模块的管理表格（范围、进度、质量、成本、沟通、风险、采购、资源、资料 + 需求登记表、项目主表）。

---

## 一、CSS 变量（必须原样使用）

```css
:root {
  --bg: #eef1f6;        --bg2: #fff;          --card: #fff;
  --card2: #f5f6fa;     --border: #dde0e8;    --text: #1a1d23;
  --text2: #5f6570;     --text3: #949aa8;     --p: #2F5496;
  --p1: #3d6cb9;        --g: #10b981;         --y: #f59e0b;
  --r: #ef4444;         --radius: 10px;
  --shadow: 0 2px 8px rgba(0,0,0,.04);        --gap: 10px;
}
```

---

## 二、页面结构（从上到下，严格此顺序）

### 2.1 顶栏 header（sticky, 50px, z-index:50）

```
[绿点呼吸灯(7px, #10b981, pulse动画)]  [🔍切换项目] [🏢部门筛选] [📊项目状态] [📅时间筛选]  [🌐时钟]
```

- 按钮类 `.btn-sm`：`padding:5px 12px; border-radius:16px; border:1px solid var(--border); background:var(--card); color:var(--text2); font-size:11px; cursor:pointer`
- 按钮 hover：`border-color:var(--p1); color:var(--p)`
- 时钟：`font-size:11px; color:var(--text3)`，每秒更新

### 2.2 第1行：6项KPI指标条

```html
<div class="kpi6">    <!-- display:grid; grid-template-columns:repeat(6,1fr); gap:10px; margin-bottom:10px -->
```

每张卡片（`.tk`）：
```
┌─────────────────────────────┐
│ ▌  [数值 24px/900/letter-spacing:-1px] │  ← 4px彩色竖线(::before伪元素, width:4px height:32px)
│    [标签 10px/600/#949aa8]   │
└─────────────────────────────┘
  padding:16px 14px; gap:12px; 渐变背景; box-shadow; border-radius:10px
```

| 位置 | 显示内容 | 渐变背景 | ::before色 |
|:----:|------|----------|:---------:|
| 1 | 项目总数（多选）/ 客户类型（单选） | `#e8f0fe→#d2e3fc` | `#3d6cb9` |
| 2 | 需求总数 | `#e6f9f0→#c8f0dd` | `#10b981` |
| 3 | 高风险残留 | `#fef9e7→#fef0c7` | `#f59e0b` |
| 4 | 任务总数 | `#fde8e8→#fcd5d5` | `#ef4444` |
| 5 | 干系人 | `#f0ebff→#e2d8fc` | `#7c3aed` |
| 6 | 采购项 | `#e0f7fa→#b2ebf2` | `#0891b2` |

### 2.3 第2-3行：左右双栏 `main2`（1fr 1fr, gap:10px）

**左栏**：一个 panel 内含两行：
```
┌─ 9 大领域指标 ──────────────────────────┐
│ .k93 grid: 3列×3行, gap:6px             │
│ ┌─────┬─────┬─────┐                    │
│ │ 🎯范围│ ⏱进度│ ✅质量│  每格.k9:       │
│ │ 80%  │ 80%  │ 81%  │  padding:10px   │
│ │ 金沙▌│ 金沙▌│ 金沙▌│  gap:6px        │
│ │ 叙永▌│ 叙永▌│ 叙永▌│  border-radius:8px│
│ ├─────┼─────┼─────┤  hover:translateY(-1px)│
│ │ 💰成本│ 💬沟通│ ⚠️风险│  ::before伪元素:  │
│ │ 70%  │ 71%  │ 59%  │  width:3px       │
│ │ …    │ …    │ …    │  height:24px     │
│ ├─────┼─────┼─────┤                    │
│ │ 📦采购│ 👥资源│ 📁资料│                    │
│ │ 79%  │ 85%  │ 77%  │                    │
│ └─────┴─────┴─────┘                    │
│ ────────────────────────                │
│ ⚠️ 实时预警信号 (药丸标签)                │
│ 📊 项目类型分布 (2卡片并排)               │
└──────────────────────────────────────────┘
```

9领域每格配色：

| 格位 | 渐变背景 | ::before色 |
|:----:|----------|:----------:|
| 🎯范围 | `#e8f0fe→#dbeafe` | `#3d6cb9` |
| ⏱进度 | `#fef3c7→#fde68a` | `#f59e0b` |
| ✅质量 | `#e6f9f0→#d1fae5` | `#10b981` |
| 💰成本 | `#fce7f3→#fbcfe8` | `#ec4899` |
| 💬沟通 | `#e0f0ff→#bfdbfe` | `#3b82f6` |
| ⚠️风险 | `#fee2e2→#fecaca` | `#ef4444` |
| 📦采购 | `#e0f7fa→#b2ebf2` | `#0891b2` |
| 👥资源 | `#f0ebff→#ddd6fe` | `#7c3aed` |
| 📁资料 | `#f3f4f6→#e5e7eb` | `#6b7280` |

每格内容：图标(18px) + 数值(20px/900) + 标签(9px/700/#5f6570) + 双项目进度条(高6px，圆角3px)

数值颜色：数值≥90%→#10b981绿色 / 70-90%→#f59e0b琥珀 / <70%→#ef4444红色

预警信号：红色药丸(严重) / 琥珀药丸(警告)，10px字号，16px圆角

项目类型分布：2列并排卡片，分别蓝底(#e8f0fe)和绿底(#e6f9f0)，左侧4px竖线

**右栏** `.side`（flex column, gap:10px）：

```
┌─ 9 维能力雷达 ─┬─ 健康度排行 ─┐     ← 内嵌 grid(1fr 1fr, gap:10px)
│  (Chart.js)    │ 🥇 叙永智慧教育│
│  雷达图         │    SaaS/托管   │
│  双项目对比     │    76%        │
│                │ 🥈 金沙一中   │
│                │    私有化部署   │
│                │    71%        │
├────────────────┴──────────────┤
│  ┌─ 薄弱环节·差距分析(2×3) ─┐ │
│  │ 资料│ 风险│ 成本│ 范围│ 任务│ 采购││
│  │ 68% │ 67% │ 70% │ 80% │ 80% │ 79% ││
│  └─────────────────────────┘ │
└──────────────────────────────┘
```

薄弱环节6张卡片配色（按顺序）：

| 卡片 | 渐变底色 | 左边框色 |
|------|----------|:-------:|
| 资料管理 | `#fef2f2→#fee2e2` | 红 #ef4444 |
| 风险控制 | `#fff7ed→#fed7aa` | 黄 #f59e0b |
| 成本回款 | `#fffbeb→#fef3c7` | 黄 #f59e0b |
| 范围确认 | `#f0f9ff→#e0f2fe` | 黄 #f59e0b |
| 任务执行 | `#f0fdf4→#dcfce7` | 蓝 #3d6cb9 |
| 采购到货 | `#eff6ff→#dbeafe` | 蓝 #3d6cb9 |

排行第1名：`#fef3c7→#fde68a` 金色渐变 + 圆形编号 `#f59e0b→#fbbf24`
排行第2名：`#f3f4f6→#e5e7eb` 银灰渐变 + 圆形编号 `#9ca3af→#c4c9d0`

### 2.4 分隔线 `.dv`

```html
<div class="dv"><div class="dl"></div><div class="dt">范围管理 · 需求追踪</div><div class="dl"></div></div>
```
左右两条线(flex:1, 1px #dde0e8) + 中间文字(11px/800/#5f6570, letter-spacing:2px)

### 2.5 需求追踪区

**第4行：4项需求指标 `.r4`**（grid:4列, gap:10px）

每张卡片（`.rt`）样式同6项KPI，配色对应前4个位置。

| 位置 | 内容 | 渐变 | ::before色 |
|:----:|------|------|:---------:|
| 1 | 需求总数 | `#e8f0fe→#d2e3fc` | `#3d6cb9` |
| 2 | 完成率 | `#e6f9f0→#c8f0dd` | `#10b981` |
| 3 | 开发中 | `#fef9e7→#fef0c7` | `#f59e0b` |
| 4 | 待确认 | `#f0ebff→#e2d8fc` | `#7c3aed` |

**第5行：4项积压分析 `.r4`**

每张卡片（`.blc`）居中文字，8px圆角，4px左侧竖线。

| 内容 | 渐变背景 | 左边框色 |
|------|----------|:-------:|
| 积压需求 | `#fde8e8→#fcd5d5` | 红 #ef4444 |
| 待确认 | `#fef9e7→#fef0c7` | 黄 #f59e0b |
| 平均处理周期 | `#e8f0fe→#d2e3fc` | 蓝 #3d6cb9 |
| 完成速率 | `#e6f9f0→#c8f0dd` | 绿 #10b981 |

**第6行：3个图表 `.r3`**（grid:3列, gap:10px）

| 图表 | 类型 | 配置 |
|------|------|------|
| 📈 需求累计完成趋势 | line | 双线(#4da3ff/#22d3a0)+填充, 点4px, 张力0.3, Y轴步长2 |
| 📊 需求状态分布 | doughnut | 4色(#22d3a0/#3b82f6/#f59e0b/#ef4444), borderWidth:0 |
| 📋 需求分类分布 | bar+indexAxis:'y' | 蓝色半透明柱 rgba(77,163,255,.7), 4px圆角, 不显示图例 |

每个图表面板最小高度240px。

**第7行：需求明细列表**（全宽 panel）

- 搜索框：白色+🔍图标+13px字号，聚焦时蓝边框+淡蓝外发光
- 表格：sticky表头，13px内容，hover浅蓝底色
- 列：ID(monospace) / 标题 / 类型 / 优先级(彩色标签) / 状态(彩色标签) / 来源 / 日期
- 优先级标签：高=红底 / 中=黄底 / 低=绿底（10px/700, 12px圆角）
- 状态标签：已完成=绿 / 开发中=蓝 / 待确认=黄 / 已拒绝=红（10px/600, 12px圆角）
- 表格最大高度370px，超出滚动

---

## 三、弹窗系统

### 3.1 切换项目弹窗（多选）
- 遮罩 `rgba(0,0,0,.3)`，弹窗14px圆角 + scale动画
- 搜索框 + ☑全选/☐取消按钮 + 「已选N项」
- 每个项目 ✓ 复选框(20px, 6px圆角)，已选蓝底白勾
- 选N项→第一KPI显示项目数；选1项→显示客户类型

### 3.2 部门筛选 / 项目状态弹窗
- 同款弹窗样式，单选列表，点击关闭

### 3.3 齿轮公式弹窗（⚙️）
- 每面板右上角26px圆形齿轮按钮
- hover旋转60度，点击弹出公式说明
- 弹窗等宽字体公式+数据来源表名
- 点击遮罩或✕关闭

---

## 四、计算公式与数据来源

> 全部数据来源于项目管理中各模块管理表格。

### 4.1 6项KPI

| KPI | 数据来源表 | 计算方式 |
|-----|-----------|----------|
| 项目总数 | 项目主表 | 统计项目数量；单项目模式下切换为该项目的客户类型字段 |
| 需求总数 | 需求登记表 | `COUNT(需求表)` |
| 高风险残留 | 风险登记表 | `COUNT(优先级="高" AND 状态≠"已关闭")` |
| 任务总数 | 任务跟踪表 | `COUNT(任务表)` |
| 干系人 | 沟通计划表 | `SUM(干系人数量)` |
| 采购项 | 采购计划表 | `SUM(采购条目)` |

### 4.2 9大领域

| 领域 | 来源表 | 公式 |
|------|--------|------|
| 范围 | 范围管理表 | `= 已确认模块数 / 模块总数 × 100%` |
| 进度 | 进度管理表 | `= 已完成任务数 / 任务总数 × 100%` |
| 质量 | 质量管理表 | `= 通过检查点数 / 检查点总数 × 100%` |
| 成本 | 成本管理表 | `= 已付金额 / 合同总额 × 100%` |
| 沟通 | 沟通管理表 | `= 沟通记录数 / 计划次数 × 100%` |
| 风险 | 风险管理表 | `= 已关闭风险数 / 风险总数 × 100%` |
| 采购 | 采购管理表 | `= (自研到+物联到+合作商到) / (自研总+物联总+合作商总) × 100%` |
| 资源 | 资源管理表 | `= (人员到位% + 服务器到位% + 设备到位% + 账号到位%) / 4 × 100%` |
| 资料 | 资料管理表 | `= 已完成文档数 / 文档总数 × 100%` |

### 4.3 健康度排行
```
综合得分 = (范围% + 进度% + 质量% + 成本% + 风险关闭% + 采购% + 资源% + 资料%) / 8
```
降序排列，第1🥇第2🥈

### 4.4 薄弱环节
取9领域中双项目均值最低的6项，升序排列，标注最低项目数值及差距

### 4.5 需求追踪

| 指标 | 来源表 | 计算方式 |
|------|--------|----------|
| 需求总数 | 需求登记表 | 总行数 |
| 完成率 | 需求登记表 | `状态="已完成" / 总数 × 100%` |
| 开发中 | 需求登记表 | `状态="开发中"` 计数 |
| 待确认 | 需求登记表 | `状态="待确认"` 计数 |
| 积压需求 | 需求登记表 | 等同于开发中 |
| 平均处理周期 | 需求登记表 | `AVG(完成日期 - 提出日期)` 天数 |
| 完成速率 | 需求登记表 | `已完成数 / 活跃月数` |
| 累计趋势 | 需求登记表 | 按月分组统计累计已完成数 |
| 状态分布 | 需求登记表 | 按状态字段分组统计 |
| 分类分布 | 需求登记表 | 按分类字段分组统计 |

### 4.6 项目类型分布
统计项目主表中各类型（私有化/SaaS）的项目数量

---

## 五、图表全局配置（Chart.js）

```javascript
Chart.defaults.color = '#5f6570';
Chart.defaults.borderColor = '#dde0e8';
// 所有图表: responsive: true, maintainAspectRatio: false
// 图例: position: 'bottom', usePointStyle: true
```

### 5.1 雷达图
- `scales.r`: min:0, max:100, stepSize:20
- 网格色:`#e5e7eb`，标签色:`#5f6570`
- 双数据集：蓝`#4da3ff` + 绿`#22d3a0`，半透明填充`.08`

### 5.2 折线图
- 双线+填充，点4px，张力0.3
- Y轴min:0，步长2

### 5.3 环形图
- 无边框 borderWidth:0

### 5.4 横向柱状图
- 蓝色半透明，4px圆角，无图例

---

## 六、交互规范

| 交互 | 行为 |
|------|------|
| 多选项目 | 全选/取消+搜索+✓复选框+确认后KPI聚合 |
| 搜索过滤 | 客户端即时过滤，不发起请求 |
| 齿轮按钮 | hover旋转60°，点击弹窗显示公式+来源表 |
| 弹窗关闭 | 点击遮罩/✕/取消按钮 |
| 需求表格 | sticky表头，滚动区370px，hover浅蓝 |
| 9领域卡片 | hover上浮1px，加深阴影 |
| 排行榜 | 排名按综合得分降序，金/银视觉区分 |

---

## 七、响应式断点

| 宽度 | 变化 |
|------|------|
| ≥1100px（默认） | 6列KPI / 左右双栏(1:1) / 4列需求 / 3列图表 |
| 700-1100px | 3列KPI / 单栏 / 2列需求+图表 / 9领域变2列 |
| <700px | 2列KPI / 单栏 / 1列 |

---

## 八、ai分析入口改成了顶部。

点击 [生成新预警]
  → POST /api/projects/dashboard-warnings
  → 遍历每个项目的 schema
  → 查 information_schema.tables 获取表列表
  → 逐表 COUNT(*) 获取行数
  → 拼成项目摘要文本
  → 发给 DeepSeek 分析
  → 解析 JSON 数组 [{"project_id":"...","level":"error","message":"..."}]
  → 存入 dashboard_ai_warnings 表
  → 前端渲染红/橙/蓝三色预警卡片




  以下内容为二次增加
一、单指标阈值预警
预警项	触发条件	严重级别	数据来源
领域完成率过低	任意领域 < 60%	严重	9大领域表
领域完成率偏低	任意领域 60-75%	警告	9大领域表
高风险残留超限	高风险数 ≥ 3 项	严重	风险登记表
高风险残留存在	高风险数 1-2 项	警告	风险登记表
里程碑延迟	里程碑完成率 < 70%	严重	进度管理表
回款滞后	回款率 < 60%	警告	成本管理表
文档缺失	必须归档缺失 ≥ 5 项	严重	资料管理表
需求积压	开发中需求 ≥ 总数40%	警告	需求登记表
待确认堆积	待确认 ≥ 5 条且超过7天	警告	需求登记表
沟通不足	干系人覆盖率 < 80%	警告	沟通计划表
二、趋势恶化预警
预警项	计算方式	严重级别
需求积压增长	连续2个月开发中数量上升	警告
完成速率骤降	本月完成速率 < 上月70%	严重
风险不降反增	本月高风险数 > 上月	严重
多领域下滑	3个以上领域同时下降 > 5%	严重
需求老化	某需求待确认超过30天	警告
三、差值/对比预警
预警项	计算方式	触发条件
项目间严重分化	两项目综合得分	差距 > 15%
项目间领域分化	同一领域两项目差值	> 20%
部门需求集中	某部门需求占比	> 50%



以下内容是 2026 年 6 月 24 号。

你在这个板块上给我加一个设置按钮。可以选择哪个模块、哪张表，让超管用户去设置。如果这样设置了，系统就去查所有项目对应的板块，以该表对应的记录条数作为显示。



项目看板页面的“任务总数”，我想设置成类似于“需求总数”的筛选方式：先选择对应模块，再选择对应表中满足条件的某些选项记录。这个选择应该是要选择两行。比如我有一个“高风险”的表，其中一个选项有 10 行，同时这个表还有一个选项是“已完成”的，那么“残留”的就应该是“高风险”的所有选项减去“已完成”的。

我想实现类似于这样的设置，你能给我一个类似于“需求总数”这样设置的方案吗？


项目看板页面 整体的思路是，默认情况下查看所有项目的统计。
1. 项目总数、项目需求、高风险残留任务总数、干系人。需求总数、完成率、开发中待确认，积压需求 这几个在每个板块上面加一个齿轮，点击之后弹出一个对话框，可以设置条件，选择模块再选择数据表在选择列 筛选出项目表格里面的数量，默认进入是查看所有满足顶部筛选条件项目。筛选除了包含，还有排除筛选条件支持多选。筛选出来的值，可以灵活的进行加减乘除运算，然后之前的统计方式去掉。数据点击进入这个界面，则进行查询。你给我方案，主要是逻辑这块的。
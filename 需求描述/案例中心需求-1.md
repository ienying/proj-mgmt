案例中心功能基本有了。我要做如下修改，这些修改包括
# 整体要求
调整后保持排版对齐，选择框选没选择宽度都固定。整个画像系统 风格要有点像看板一样即看起来炫酷不同业务班牌可以有不同颜色，或者加一些装饰但有不能他花哨。

# 新建和编辑画像的时候，
## 基础信息 
学校名称为下拉搜索选择，宽度固定。选择来源于新建的项目的学校名称。
学校类型，改成客户类型 幼儿园、小学、初中、中职、高中、高职、技师、教育局
其中 幼儿园、小学、初中、中职、高中、支持多选。多选了之后考虑该用户画像，考虑不同阶段画像 
位置信息，直接同步 项目管理页面 创建新项目的数据包括 省/自治区/直辖市、区/县、镇/乡、市、村、经度、纬度 数据来源于创建项目的数据，直接读取到这里显示。项目管理页面做了更新。那么画像这里同步更新。
硬件与网络信息 无线覆盖和堡垒机选项放到一起

#### 描述改成多行文本。
## 科室名称
默认的科室名称除了校领导，其他的都支持修改因为，每个学校对应名称有可能不一样但是也要考虑能对比情况。
## 科室 模块匹配，
1、改成下拉搜索，模块名称 来源于 系统设置-基础数据-产品名称-模块名称，注意模块是显示前端中文名称
2、模块匹配所有的文本输入字段都为多行文本。包括选择都要对齐。
3、每个板块编辑的时候每个板块加个背景颜色。


系统设置 里增加一个接入大模型配置。
dock 栏 上的周报去掉 改成ai 当点击了这个按钮后 弹出选择框 选择是否 加上 项目项目数据 加画像数据生成 默认有科室数据 生成ai对应的科室报告 ，总览报告。

我要收集有没有标准








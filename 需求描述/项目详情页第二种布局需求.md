## 背景说明

1. 本次修改的内容为项目详情页。跟进 index-54.html 完整的复刻，如果系统上面有数据不匹配的，可以先写成静态数据，我后期再告诉你怎么修改。其他的页面不要改。如果你觉得现在新改的这个有必要增加单独的文件，那你也可以根据需要进行增加单独的文件。

本次任务：在进入项目详情前，弹出一个布局选择弹窗，新增一个「阶段式布局」选项。两个布局共存，可随时切换。
你做完之后记得提交 GitHub。这是一个重大的更新，因此需要重新起一个分支，并给之前的主分支做一个重要的标记。
本次任务：只改进项目详情页，需求描述下的文件index-v54.html完整的复刻阶段式布局。
2. 数据来源策略
- **优先使用系统数据**：项目数据（Hero 统计、产品清单、任务列表、子页面表格等）由后台管理表格系统提供，AI 需定义好数据结构字段格式和渲染函数，数据的具体值由系统在运行时注入。
- **数据不可得时写静态模拟数据**：如果某些表格数据找不到对应来源，则先写静态模拟数据占位，保证页面可正常预览，后续逐步替换为真实数据。

### 3. 原有项目创建逻辑必须保留

## 交互流程

```
项目列表页 → 点击「进入详情」
                  ↓
          弹出布局选择弹窗
          ┌──────────────────┐
          │  选择查看方式      │
          │                  │
          │  ○ 管理式布局      │
          │  ● 阶段式布局（推荐）│
          │                  │
          │      [ 确 定 ]    │
          └──────────────────┘
                  ↓
          进入所选布局的项目详情页
                  ↓
    页面顶部栏右侧保留「布局切换」按钮，随时切换

### 关于管理式布局（旧版）

管理式布局是**已有的旧版布局**，不需要在本提示词中重新描述或实现。它已经存在，用户早已在使用。本提示词的目的是生成**阶段式布局（V48 新版）**。

弹窗的作用只是让用户在进入项目详情时选择要看哪个版本——旧版管理式或新版阶段式。两套布局需共用同一套数据源（`taskData` / `subContentData` / `panelData`），切换布局不破坏数据一致性。